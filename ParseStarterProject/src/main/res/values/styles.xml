<!--
  ~ Copyright (c) 2015-present, Parse, LLC.
  ~ All rights reserved.
  ~
  ~ This source code is licensed under the BSD-style license found in the
  ~ LICENSE file in the root directory of this source tree. An additional grant
  ~ of patent rights can be found in the PATENTS file in the same directory.
  -->
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/userBottomBar_focused</item>
        <item name="colorPrimaryDark">@color/userBottomBar</item>
        <item name="colorAccent">@color/dark_red</item>
    </style>

    <!-- Splash theme to show when app configures -->
    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_bg</item>
    </style>

    <!-- Activity with no Title -->
    <style name="ThemeOverlay.MyNoTitleActivity" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Dark Buttons -->
    <style name="ThemeOverlay.MyDarkButton" parent="ThemeOverlay.AppCompat.Dark">
        <item name="colorButtonNormal">@color/userBottomBar</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <!--Red Buttons-->
    <style name="ThemeOverlay.MyRedButton" parent="ThemeOverlay.AppCompat.Dark">
        <item name="colorButtonNormal">@color/dark_red</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <!--action bar-->
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="colorPrimary">@color/dark_red</item>
        <item name="colorPrimaryDark">@color/dark_red</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="spinner_addr_style" >
        <item name="android:background">@drawable/spinner_addr</item>
        <item name="android:paddingRight">15dp</item>
    </style>

</resources>
